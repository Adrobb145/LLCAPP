// shared/Spark.jsx — inline sparkline
export default function Spark({data,color="#FF6B2C",h=60}){if(!data||data.length<2)return null;const w=100,max=Math.max(...data)*1.05,min=Math.min(...data)*.95,r=(max-min)||1;const pts=data.map((v,i)=>[(i/(data.length-1))*w,h-((v-min)/r)*(h-8)-4]);const path=pts.map((p,i)=>`${i?"L":"M"}${p[0].toFixed(1)},${p[1].toFixed(1)}`).join(" ");return(<svg className="spark" viewBox={`0 0 ${w} ${h}`} preserveAspectRatio="none"><path d={path} fill="none" stroke={color} strokeWidth="1.5" vectorEffect="non-scaling-stroke"/>{pts.map((p,i)=><circle key={i} cx={p[0]} cy={p[1]} r="1.4" fill={color}/>)}</svg>);}
