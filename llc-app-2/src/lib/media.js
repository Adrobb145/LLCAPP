// lib/media.js — client-side image compression for progress photos
export function compressImg(file,max=900,q=0.72){return new Promise(res=>{try{const rd=new FileReader();rd.onerror=()=>res(null);rd.onload=()=>{const img=new Image();img.onload=()=>{let w=img.width,h=img.height;if(w>h){if(w>max){h=Math.round(h*max/w);w=max;}}else{if(h>max){w=Math.round(w*max/h);h=max;}}const cv=document.createElement("canvas");cv.width=w;cv.height=h;cv.getContext("2d").drawImage(img,0,0,w,h);try{res(cv.toDataURL("image/jpeg",q));}catch(e){res(rd.result);}};img.onerror=()=>res(rd.result);img.src=rd.result;};rd.readAsDataURL(file);}catch(e){res(null);}});}
